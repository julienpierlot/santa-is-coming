section.hero.is-primary
  .hero-body
    .hero-container
      h1.title List of houses

= @houses.each do |house|
  section.section
    .tile.is-ancestor
      .tile.is-parent
        .tile.is-child.box
          .content
            h3.title
              =link_to "##{house.id}", house_path(house)
            p
              |Address: 
              = house.address
            p
              |Nb of children: 
              = house.children.count
